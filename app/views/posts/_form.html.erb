<%= form_for @post do |f| %>
  <%= f.error_messages %>
  <% if params[:topic] %>
    <%= f.hidden_field :topic_id, :value => params[:topic] %>
  <% end %>
  
  <p>
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </p>
  <p><%= f.submit %></p>
<% end %>
