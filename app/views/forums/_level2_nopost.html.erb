<tr>
  <td class="tcat" colspan="5">
    <div><%= link_to h(child.title), forum_path(child.id) %></div>
    <% if child.description? %>
      <div class="smallfont"><%= h(child.description) %></div>
    <% end %>
  </td>
</tr>
